import numpy as np
class level:
    def __init__(self,levelName,gameResolution):
        self.name=levelName
        self.map=np.zeros((gameResolution[0],gameResolution[1]))
    def save():
        ## Function will save the level created witch can be imported to the main game 
        pass
    def load():
        ## Function will load level files .. so that level edditing can be saved and resumed
        pass
